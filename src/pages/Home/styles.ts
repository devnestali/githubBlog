import styled from "styled-components";

export const HomeContainer = styled.main`
    min-width: 86.4rem;
    
    display: flex;
    flex-direction: column;
    gap: 7.2rem;

    padding: 0 32.5rem;

`;