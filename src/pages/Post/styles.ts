import styled from "styled-components";

export const PostContainer = styled.div`
    display: flex;
    flex-direction: column;
    gap: 2.5rem;

    margin-top: -10rem;

    &:last-child {
        margin-bottom: 3rem;
    }
`;